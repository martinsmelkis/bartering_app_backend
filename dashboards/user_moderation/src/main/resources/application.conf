plugin {
    ktor {
        deployment {
            port = 8085
        }
        application {
            modules = [ app.bartering.dashboard_user_moderation.ApplicationKt.module ]
        }
        server {
            host = "0.0.0.0"
        }
    }
}
